package com.essa.interview;

import com.essa.interview.Student;

public class StudentMain {
    public static void main(String[] args) {

        Student student = new Student();
        student.name = "essa";
        student.age = 65;

        System.out.println(student.name);

    }
}

//Polymophism - ability to change(morph)/transform something into many forms
// e.g shape  ==> can be moulded into a circle, triangle etc